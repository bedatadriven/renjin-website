---
title: Renjin Blog RSS feed
---
{
    "posts": [ {% for post in site.posts limit:10 %}
    {
        "title": "{{ post.title|escape }}",
        "date": "{{ post.date | date: '%Y-%m-%d' }}",
        "url": "{{ site.url }}{{ post.url }}",
        "excerpt": "{{ post.excerpt|escape }}"
    }{% if forloop.last %}{% else %},{% endif %}{% endfor %}
    ]
}

